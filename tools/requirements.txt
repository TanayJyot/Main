# Phase 0-2 of the SHHQ migration (see tools/README.md).
# Deliberately excludes torch/diffusers: nothing here trains anything.

numpy>=1.24
opencv-python-headless>=4.8
Pillow>=10.0

# Pose fidelity scoring and corpus filtering.
mediapipe>=0.10.9
pose-format>=0.9.0

# Mask generation. rembg pulls onnxruntime; swap for segment-anything if you
# prefer the SAM backend in make_masks.py (Apache-2.0, needs torch).
rembg>=2.0.50

tqdm>=4.66
